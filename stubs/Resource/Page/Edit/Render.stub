it({{ DESCRIPTION }}, function () {
    $record = {{ MODEL_SINGULAR_NAME }}::factory()->create();

    livewire(Edit{{ MODEL_SINGULAR_NAME }}::class, ['record' => $record->getRouteKey()])
        ->assertSuccessful();
}){{ RESOLVED_GROUP_METHOD }};
