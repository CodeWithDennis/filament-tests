it({{ DESCRIPTION }}, function (string $action) {
    livewire(List{{ MODEL_PLURAL_NAME }}::class)
        ->assertActionVisible($action);
})->with({{ INDEX_PAGE_VISIBLE_HEADER_ACTIONS }}){{ RESOLVED_GROUP_METHOD }};
