it({{ DESCRIPTION }}, function (string $action) {
    livewire({{ RESOURCE_LIST_CLASS }})
        ->assertTableBulkActionExists($action);
})->with({{ RESOURCE_TABLE_BULK_ACTIONS }}){{ RESOLVED_GROUP_METHOD }};
