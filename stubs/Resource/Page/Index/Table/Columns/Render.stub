it({{ DESCRIPTION }}, function (string $column) {
    livewire({{ RESOURCE_LIST_CLASS }})
        ->assertCanRenderTableColumn($column);
})->with({{ RESOURCE_TABLE_INITIALLY_VISIBLE_COLUMNS }}){{ RESOLVED_GROUP_METHOD }};
