it({{ DESCRIPTION }}, function (string $column) {
    livewire({{ RESOURCE_LIST_CLASS }})
        ->assertCanNotRenderTableColumn($column);
})->with({{ RESOURCE_TABLE_TOGGLED_HIDDEN_BY_DEFAULT_COLUMNS }}){{ RESOLVED_GROUP_METHOD }};
