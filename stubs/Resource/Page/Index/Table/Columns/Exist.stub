it({{ DESCRIPTION }}, function (string $column) {
    livewire(List{{ MODEL_PLURAL_NAME }}::class)
        ->assertTableColumnExists($column);
})->with({{ RESOURCE_TABLE_COLUMNS }}){{ RESOLVED_GROUP_METHOD }};
