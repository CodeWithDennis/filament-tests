it({{ DESCRIPTION }}, function (string $action, string $url) {
    livewire({{ RESOURCE_LIST_CLASS }})
        ->assertTableActionHasUrl($action, $url)
        ->assertActionShouldNotOpenUrlInNewTab($action);
})->with({{ RESOURCE_TABLE_ACTIONS_WITH_URL }}){{ RESOLVED_GROUP_METHOD }};
