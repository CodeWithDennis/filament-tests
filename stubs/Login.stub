it('can render the login page', function () {
    livewire({{ LOGIN_ROUTE_ACTION }}::class)
        ->assertRedirect({{ PANEL_PATH }});

    Auth::logout();

    livewire({{ LOGIN_ROUTE_ACTION }}::class)
        ->assertSuccessful();
})->group('rendering');
