it('has table action', function (string $action) {
    livewire(List{{ MODEL_PLURAL_NAME }}::class)
        ->assertTableActionExists($action);
})->with({{ RESOURCE_TABLE_ACTIONS }}){{ RESOLVED_GROUP_METHOD }};
