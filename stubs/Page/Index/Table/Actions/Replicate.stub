it('can replicate records', function () {
    $record = {{ MODEL_SINGULAR_NAME }}::factory()->create({{ MULTI_TENANCY_FACTORY_ATTRIBUTES }});

    livewire(List{{ MODEL_PLURAL_NAME }}::class)
        ->callTableAction(ReplicateAction::class, $record);

    expect({{ MODEL_SINGULAR_NAME }}::count())->toBe(2);
}){{ RESOLVED_GROUP_METHOD }};
