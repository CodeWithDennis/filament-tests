it('can list records on the index page', function () {
    $records = {{ MODEL_SINGULAR_NAME }}::factory({{ DEFAULT_FACTORY_COUNT }})->create();

    livewire(List{{ MODEL_PLURAL_NAME }}::class)
        ->assertCanSeeTableRecords($records);
})->group('table');
