it('can list records on the index page with pagination', function () {
    $records = {{ MODEL_SINGULAR_NAME }}::factory({{ DEFAULT_PAGINATED_RECORDS_FACTORY_COUNT }})->create({{ MULTI_TENANCY_FACTORY_ATTRIBUTES }});

    livewire(List{{ MODEL_PLURAL_NAME }}::class){{ LOAD_TABLE_METHOD_IF_DEFERRED }}
        ->call('gotoPage', 2)
        ->assertCanSeeTableRecords($records->skip({{ DEFAULT_PER_PAGE_OPTION }}));
})->group('table');
