it('can render the view page', function () {
    $record = {{ MODEL_SINGULAR_NAME }}::factory()->create();

    livewire(View{{ MODEL_SINGULAR_NAME }}::class, ['record' => $record->getRouteKey()])
        ->assertSuccessful();
}){{ RESOLVED_GROUP_METHOD }};
