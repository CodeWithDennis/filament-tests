it('can reset table filters', function () {
    $posts = {{ MODEL_SINGULAR_NAME }}::factory()->count(3)->create();

    livewire({{ MODEL_SINGULAR_NAME }}Resource\Pages\List{{ MODEL_PLURAL_NAME }}::class)
        ->resetTableFilters()
        ->assertCanSeeTableRecords($posts);
});